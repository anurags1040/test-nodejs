apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "circle-nodejs-chart.fullname" . }}-test-connection"
  labels:
    {{- include "circle-nodejs-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "circle-nodejs-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
